export interface FilterOptions {
  search?: string;
  size?: string;
  colors?: string[];
  style?: string[];
  material?: string;
}

export interface SelectOption {
  value: string;
  label: string;
}

